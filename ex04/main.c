#include <stdio.h>
#include <stdlib.h>
int ft_str_is_lowercase(char *str);
int main(void)
{
    char *str;

    str = "ABRCGDTRE";
    ft_str_is_lowercase(str);

	/*if(str == "1")
	{
		printf("%s", str);
		printf("só maiscula");
	}
	else
	{
		printf("%s", str);
		printf("não maiscula");
	}*/

}